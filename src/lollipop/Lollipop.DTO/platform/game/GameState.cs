namespace com.riotgames.platform.game
{
    public enum GameState
    {
        JOINING_CHAMP_SELECT,
        PRE_CHAMP_SELECT,
        CHAMP_SELECT,
        POST_CHAMP_SELECT,
        START_REQUESTED,
        IN_PROGRESS,
        TEAM_SELECT,
        TERMINATED,
        TERMINATED_IN_ERROR
    }
}